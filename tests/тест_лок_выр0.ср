#влечь (
	"ведать.ср"
	"выход.ср"
)

_асм "
вход глав
глав:"

ведать эээ world ,
выйти с кодом 77 .

пусть г: ч32 = 1,
пусть уг: *ч32 = &г,

лик Чел (
	ф и о: стр
	возраст: ц8
)
лик Начальник (
	раб1: лик Чел
	раб2: лик Чел
	раб3: &лик Чел
)
лик Завод (
	нач1: &лик Начальник
	нач2: &лик Начальник
	нач3: &лик Начальник
	нач4: &лик Начальник
	нач5: лик Начальник
)
лик Заводы (
	зав1: &лик Завод
	зав2: &лик Завод
	зав3: &лик Завод
	зав4: лик Завод
)
пусть гчел: &лик Чел = *{0, 0, 0, 0},
пусть звы: &лик Заводы = *{},

фц глав(ч88: ч8 аргов: ч32 арги: *стр) тлен (
	пусть (
		а: [ч32]
		int_ptr: **ч32
		оа об ов: в32
		да дб дв: в64
		ч в a b c q w e r t y u i o p s d f g h j k l z x v n m: ч32
		ux uy uz ua ub uc: ц32
	)

	а[ч88 + аргов]++,
	(1, (аргов++, 1), 1),

	а[ч
	  + ((ч88++, 2)
	  + (ч++, 2))
	  + !~!~(а[ч+ч]++, (а[ч*3]++, 1))
	]++,
	а[0 + (ч88++, ч)]++,

	а[(ч88++, 1) && (ч++, 0)]++,

	оа = ч + 123.123,
	ч = 123.123,


	a = x + -10.
	a = x + y + 10.,
	(a = (
	    (((2.0 * 1.0) * (((x * 1.0) + (y + 0.0)) + (5.0 * 2.0))) +
	    ((1.0 + 0.0) * (((a * 1.0) + (b + 0.0)) + (1.5 * 2.0)))) +
	    (7.5 * 2.0) +
	    (30.0 / 2.0) -
	    15.0 +
	    (0.0 * x * y * a * b * 1000.0)
	)),

	(а = 16),
	(а = (((a + b) + c) + 10)),
	(а = (((1 * x) + (3 * x)) + 12)),
	(а = ((a + b) + 20)),
	(а = ((15 + x) + y)),
	(а = (((3 * k) + (5 * k)) + 12)),
	(а = (((((a + (2 * b)) + (4 * b)) + c) + 8) + (6 * b))),
	(в = (((2 * ((x + y) + 10.)) + (1 * ((a + b) + 3))) + 15)),
	(а = ((28 + (a + (b + (c + d)))) + (e + f))),
	(а = (((x + 21) + y) + (z + t))),
	(а = (((a + (21 + (b + c))) + (d + e)) + f)),
	(а = (((36 + x) + y) + z)),
	(а = ((((((a + 21) + b) + c) + d) + e) + f)),
	(а = (((21 + (x + y)) + z) + (t + u))),
	(а = (((36 + (a + b)) + (c + (d + e))) + (f + g))),
	a = x + 10 - 9,
	;a = x * 10 / a / 9,
	;a = 10 / x / (100 / x) / 10,
	a = (x + y / z) / (a + b / c),
	a = (x + y / z) / (a + b / c),
	a = (x + x) * (x + x) + 10 - 9,
	а = 10 / x,
	а = x / 8,
	а = x / -8,
	а = x / 10,
	а = x / -10,
	a = x / (x / (x / (x / (x / (x / x))))),
	a = x / (x / (x / (x / (x / (x / x))))),
	a = x * 32 + 32 * x,
	a = x >> 3,
	a = uz >> (ux /uy),

	пусть ( в32_0 в32_1: в32 в64_0 в64_1: в64 )
	a = (в32_0 + в32_1) + в64_0,

	a = (x + y) + ((x << 1) / y),

	а[ч + 19]++,
	int_ptr[ч + (19 + x)]++,

	ux = (x = 0, uy) >> (y = 0, 32),

	пусть (
		fa fb: (ч32 ч32 ! ч32)
		чел: & лик Чел
		чел2: лик Чел
		нач: лик Начальник
		заводы: &лик Заводы
		звды: лик Заводы
	)
	;(1, 2) |> x ? fa : fb
	чел-@и++,
	гчел-@и++,
	чел2->о++,

	x = чел-@и,
	y = гчел-@и,
	z = чел2->о,
	z = (*чел2)-@возраст,
	z = нач->раб2->и,
	x = 0 << z,
	x = z << 0,
	x = заводы-@зав3-@нач3-@раб3-@возраст,
	x = звды->зав2-@нач3-@раб3-@возраст,
	x = звды->зав2-@нач5->раб3-@и,
	x = звды->зав4->нач5->раб1->и,
	x = заводы-@зав4->нач3-@раб3-@возраст,
	x = (*заводы-@зав4->нач5)-@раб3-@возраст,
	x = (*заводы-@зав3-@нач5)-@раб3-@возраст,
	x = (*(*заводы-@зав4->нач5)-@раб1)-@возраст,
)
