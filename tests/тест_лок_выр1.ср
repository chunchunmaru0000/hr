#влечь (
	"ведать.ср"
	"выход.ср"
)

_асм "
вход глав
глав:"

ведать эээ world ,
выйти с кодом 77 .

пусть г: ч32 = 1,
пусть уг: *ч32 = &г,

лик Чел (
	ф и о: стр
	возраст: ц8
)
лик Начальник (
	раб1: лик Чел
	раб2: лик Чел
	раб3: &лик Чел
)
лик Завод (
	нач1: &лик Начальник
	нач2: &лик Начальник
	нач3: &лик Начальник
	нач4: &лик Начальник
	нач5: лик Начальник
)
лик Заводы (
	зав1: &лик Завод
	зав2: &лик Завод
	зав3: &лик Завод
	зав4: лик Завод
)
пусть гчел: &лик Чел = *{0, 0, 0, 0},
пусть звы: &лик Заводы = *{},

фц глав(ч88: ч8 аргов: ч32 арги: *стр) тлен (

	пусть (
		а: [ч32]
		int_ptr: **ч32
		оа об ов: в32
		да дб дв: в64
		ч в a b c q w e r t y u i o p s d f g h j k l z x v n m: ч32
		ux uy uz ua ub uc: ц32
	)


	оа = ч + 123.123,
	ч = 123.123,


	a = x + -10.
	a = x + y + 10.,
	(a = (
	    (((2.0 * 1.0) * (((x * 1.0) + (y + 0.0)) + (5.0 * 2.0))) +
	    ((1.0 + 0.0) * (((a * 1.0) + (b + 0.0)) + (1.5 * 2.0)))) +
	    (7.5 * 2.0) +
	    (30.0 / 2.0) -
	    15.0 +
	    (0.0 * x * y * a * b * 1000.0)
	)),

	(а = 16),
	(а = (((a + b) + c) + 10)),
	(а = (((1 * x) + (3 * x)) + 12)),
	(а = ((a + b) + 20)),
	(а = ((15 + x) + y)),
	(а = (((3 * k) + (5 * k)) + 12)),
	(а = (((((a + (2 * b)) + (4 * b)) + c) + 8) + (6 * b))),
	(в = (((2 * ((x + y) + 10.)) + (1 * ((a + b) + 3))) + 15)),
	(а = ((28 + (a + (b + (c + d)))) + (e + f))),
	(а = (((x + 21) + y) + (z + t))),
	(а = (((a + (21 + (b + c))) + (d + e)) + f)),
	(а = (((36 + x) + y) + z)),
	(а = ((((((a + 21) + b) + c) + d) + e) + f)),
	(а = (((21 + (x + y)) + z) + (t + u))),
	(а = (((36 + (a + b)) + (c + (d + e))) + (f + g))),
	a = x + 10 - 9,
	;a = x * 10 / a / 9,
	;a = 10 / x / (100 / x) / 10,
	a = (x + y / z) / (a + b / c),
	a = (x + y / z) / (a + b / c),
	a = (x + x) * (x + x) + 10 - 9,
	а = 10 / x,
	а = x / 8,
	а = x / -8,
	а = x / 10,
	а = x / -10,
	a = x / (x / (x / (x / (x / (x / x))))),
	a = x / (x / (x / (x / (x / (x / x))))),
	a = x * 32 + 32 * x,
	a = x >> 3,
	a = uz >> (ux /uy),

	пусть ( в32_0 в32_1: в32 в64_0 в64_1: в64 )
	a = (в32_0 + в32_1) + в64_0,

	a = (x + y) + ((x << 1) / y),


	ux = (x = 0, uy) >> (y = 0, 32),
эээ:
	пусть (
		fa fb: (ч32 ч32 ! ч32)
		чел: & лик Чел
		чел2: лик Чел
		нач: лик Начальник
		заводы: &лик Заводы
		звды: лик Заводы
	)

	x = чел-@и,
	y = гчел-@и,
	z = чел2->о,
	z = (*чел2)-@возраст,
	z = нач->раб2->и,
	x = 0 << z,
	x = z << 0,
	x = заводы-@зав3-@нач3-@раб3-@возраст,
	x = звды->зав2-@нач3-@раб3-@возраст,
	x = звды->зав2-@нач5->раб3-@и,
	x = звды->зав4->нач5->раб1->и,
	x = заводы-@зав4->нач3-@раб3-@возраст,
	x = (*заводы-@зав4->нач5)-@раб3-@возраст,
	x = (*заводы-@зав3-@нач5)-@раб3-@возраст,
	x = (*(*заводы-@зав4->нач5)-@раб1)-@возраст,
	x = x > y,
	x = x > (y / z),
	x = (y / z) > x,
	;x = y > z && x > (y / z) && x > z,
	x = y && (x / z) && a && c,

	x = 2 <= x && 1 >= x,
	x = a && b && 2 >= x && 1 <= x && c,
	a = c || b || d || a == c,
	a = c && b && d && a == c,
	a = (c || b) && (a || d),
	a = (c && b) || (a && d),
	a =
	 c && b && d && a == c ||
	 c && b && d && a == c,

	x = (a > 2) && (b < 5) || (c == 8) && (d != 1) || (a >= b) && (c < 10),
	x = (a != 3) || (b > 1) && (c <= 9) || (d == 2) && (a < c) || (b != d),

	x = (a > b) && ((c < d) || (a == c)) && (b != 2),
	x = (a == 5) || ((b < c) && (d > 1)) || (c != 8),

	x = (a > 1) && (b < 10) || (c >= 5) && (d <= 3) || (a == b) && (c != d) || (b > a),
	x = (a != b) || (c == d) && (b > 2) || (d < c) && (a >= 4) || (c <= 7) && (d != 1),

	; фц глав(ч88: ч8 аргов: ч32 арги: *стр) тлен
	;x = глав(1 1.1 0),
	;x = фч(),
	x = ((x + x) / (y + y)) / ((x + x) / (y + y)),
	x = глав(1.1 фч(1) 0),
	x = глав(1.2 фч(1) 0),
	x = глав(1.3 фч(1) 0),
	x = глав(1.4 фч(1) 0),
	x = глав(1.5 фч(1) 0),
	x = глав(1.6 фч(1) 0),
	x = глав(1.7 фч(1) 0),

	x = (*(*звды->зав4->нач5)-@раб1)-@возраст,
	x = (*(*заводы-@зав4->нач5)-@раб1)-@возраст,
	(*возд_уч())->возраст = 10,
	возд_ч()->возраст = 10,
	заводы-@зав3-@нач3-@раб3-@возраст = 11,
	звды->зав2-@нач3-@раб3-@возраст = 11,
	звды->зав2-@нач5->раб3-@и = 11,
	звды->зав4->нач5->раб1->и = 11,
	заводы-@зав4->нач3-@раб3-@возраст = 11,
	(*заводы-@зав4->нач5)-@раб3-@возраст = 11,
	(*заводы-@зав3-@нач5)-@раб3-@возраст = 11,
	(*(*заводы-@зав4->нач5)-@раб1)-@возраст = 11,
	*(() |> возд_ук_ч32) = 12,

	пусть (
		массив: [ч32 2]
		массив_з: [&лик Заводы 2]
	)
	массив[x] = 13,
	массив_з[x]-@зав2-@нач5->раб3-@возраст = 14,
	массив_з[10]-@зав2-@нач5->раб3-@возраст = 14,
	массив_з[0]-@зав2-@нач5->раб3-@возраст = 14,

	а[ч + 19]++,
	int_ptr[ч + (19 + x)]++,
	а[ч88 + аргов]++,
	(1, (аргов++, 1), 1),
	а[ч
	  + ((ч88++, 2)
	  + (ч++, 2))
	  + !~!~(а[ч+ч]++, (а[ч*3]++, 1))
	]++,
	а[0 + (ч88++, ч)]++,
	а[(ч88++, 1) && (ч++, 0)]++,
	чел-@и++,
	гчел-@и++,
	чел2->о++,
	int_ptr++,
	(*int_ptr)++,
	(**int_ptr)++,

	++массив_з[x]-@зав2-@нач5->раб3-@возраст,
	массив_з[x]-@зав2-@нач5->раб3-@возраст++,
	x = ++массив_з[x]-@зав2-@нач5->раб3-@возраст,
	x = массив_з[x]-@зав2-@нач5->раб3-@возраст++,

	глав(1.7 фч(1) 0),
	глав(1.7 фч(1) 0),
	;x = &массив_з[x]-@зав2-@нач5->раб3-@возраст,
	a = a > b || a > c? (b = 10, 10) : (c = 11, 11),

	a ? a = 0 : a = 1,
	a ? a = 3 : 0,
	a ? 0 : a = 2,
	x = (y = 10),
	а[ч88]++,
	x = возд_ч8() + 123,
	x = возд_ч16() + 123,
	x = !!(x - 19),
	x = !!(x + 19),
	x = (x - 19) && (x + 19),

	x == 10
	?? ( x = 123 )
	:: ( y = 312, z = 312)

	x == 11
	?? ( y = 1, z = 2 )

	x == 12
	?? ( y = 3, z = 4 )
	:? x == 13
	?? ( y = 3, z = 4 )
	:: ( y = 5, z = 6 )
	x == 14
	?? ( y = 7, z = 8 )
	:? x == 15
	?? ( y = 9, z = 0 )

	a =
		x == 10 ? y == 10 ? 1 : 0 :
		x == 11 ? 2 :
		x == 12 ? 3 :
		4,
	(1, 2)
	|> x ? fa : fb,
	фч..(4 5 6),
	x = фч..(7 8 9),
	фч..(1 фч..(2 3 4) 5),
	фч..(1, (2, 3), 4),
	фч..(1),
)

фц возд_уч() *лик Чел ()
фц возд_ч() лик Чел ()
фц возд_ук_ч32() *ч32 ()
фц возд_ч8() ч8 ()
фц возд_ч16() ч16 ()

фц фч() ч32 ()
фц фч(a б: ч32) ч32 ()
фц фч(a: ч32) ч32 (
	a = фч(1),
	a = глав(1.2 фч(1) 0),

	пусть (
		лист_указателей: лик УЛист
		лист_байт: лик БЛист
	)
	a = лист_указателей.под_номером(1)
	a = лист_байт.под_номером(1)

	пусть (
		м: [ч32 10]
		мл: [&лик Чел 10]
		а: ч32
	)
	а = (а + а) / м[10],
	а = (а + а) / мл[10]-@возраст,
	мл[10]-@возраст = а,
	а = &мл[10]-@возраст,
)

лик УЛист (
	нч: **тлен
	темп : ц32
	вместимость : ц32
	размер : ц32
)
лик БЛист (
	нч: стр
	темп : ц32
	вместимость : ц32
	размер : ц32
)

;void *plist_get(struct PList *l, uint32_t i) { return l->st[i]; }
фц под_номером (л: лик УЛист ш: ц32 ) *тлен ( ;;воздать л->нч[ш];; )
фц под_номером (л: лик БЛист ш: ц32 ) *тлен ( ;;воздать л->нч[ш];; )
