_асм "
вход здесь
здесь:
"
;;
M:
    mov rax, M
    mov rsi, rax
    add rsi, 5
    add rax, 7
    jmp rax
1 passes, 20 bytes.
    48c7 c000 0000 00
    48 89c6
    4883 c605
    4883 c007
    ffe0
;;
#се ведать(буки) (# _асм
"
ведать:
    быть рах чбайт ведать
    быть рси рах
    плюс рси 20
    плюс рах " + (18 + 1 + 20) ;; (размера(#"буки"#) + 20) ;;+ "
    идти рах
; здесь должно быть рах
пусть байт \"" + #"буки"# + "\" 10
    быть еах 1
    быть еди 1
    быть едх " + (18 + 1) ;;размера(#"буки"#) ;;+ "
    сзов
"
#)
ведать гойда сво,

_асм "
    быть еах 60
    быть еди 77
    сзов
"
